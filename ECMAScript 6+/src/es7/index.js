let numbers = [1, 2, 3, 7, 8]

if (numbers.includes(9)){   // includes para verificar si un elemento dentro de un array existe
                            //determina si una cadena de texto puede ser encontrada dentro de otra cadena de texto, devolviendo true o false según corresponda
    console.log('Si se encuentra el valor 7')
} else {
    console.log('UPS no está :(')
}

let base = 4
let exponent = 3
let result = base** exponent //elavar en potencia

console.log(result)